<UserControl x:Class="MyGame.Game.Views.Characters.CharacterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyGame.Game.Views.Characters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Name="CharacterArea" Grid.Row="0">
            <Border Background="Black">
                <Grid>
                    <Grid.Background>
                        <ImageBrush ImageSource="./../../../Ressources/Images/Control/spriteBackground.jpg" Stretch="UniformToFill"/>
                    </Grid.Background>
                </Grid>
            </Border>
            <Image Name="CellItem" Source="{Binding MapViewModel.SpriteFullPath}"></Image>
        </Grid>
        <Grid Name="kwy" Grid.Row="1" Background="LightPink">

            <Label Name="key" Content="{Binding MapViewModel.MapElement.Key}"></Label>
        </Grid>
    </Grid>
</UserControl>
